export const STORAGE_KEYS = {
  HISTORY: 'gradient_history',
  SETTINGS: 'user_settings',
  CURRENT_SESSION: 'current_session',
} as const;

export const MAX_HISTORY_DEFAULT = 100;
export const LINEAR_SAMPLE_COUNT_DEFAULT = 10;
export const SCREENSHOT_DEBOUNCE_MS = 300;
